ARG BASE_IMAGE=python:3.11-slim
FROM ${BASE_IMAGE}

WORKDIR /workspace/vllm-ascend
COPY .ci/requirements.txt /tmp/ci-requirements.txt
RUN python -m pip install --no-cache-dir -r /tmp/ci-requirements.txt

CMD ["bash"]
