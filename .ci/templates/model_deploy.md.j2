{{ generated_warning }}

# {{ title }}

## 1. 文档概述

{{ description }}

- Case: `{{ name }}`
- Level: `{{ level }}`
- Owner: `{{ owner }}`
- Audience: {{ audience }}
- Difficulty: {{ difficulty }}
- Tags: {{ tags }}

## 2. 环境要求

### Hardware

{{ hardware }}

### Software

{{ software }}

## 3. 模型信息

{{ model_requirements }}

## 4. 部署拓扑

{{ topology }}

Jenkins runtime 每次 build 只启动一个 Docker 容器。容器级卡池和端口池由 `.ci/scripts/with_runtime_allocation.py` 在宿主机分配；容器内 runner 再按 `requirements.hardware.card_count={{ card_count }}` 为本 case 分配子卡集和端口。

## 5. 环境变量

```bash
{{ env_exports }}
```

## 6. 启动服务命令

### vLLM 命令

```bash
{{ serve_command }}
```

### Docker runtime 示例

```bash
{{ docker_command }}
```

Docker 配置：

{{ docker_runtime }}

## 7. vLLM config 示例

```yaml
{{ vllm_config }}
```

## 8. 服务验证

```bash
{{ readiness_command }}
```

```bash
{{ smoke_command }}
```

## 9. Benchmark 验证

```bash
{{ benchmark_command }}
```

## 10. Accuracy 验证

```bash
{{ accuracy_command }}
```

## 11. 参数说明

{{ parameter_table }}

## 12. 停止服务

```bash
{{ stop_command }}
```

## 13. 注意事项

- Jenkins 默认 `RUN_ASCEND=false`，不会在静态流程中启动真实模型。
- A2/A3 runtime 均采用单容器内并发，不在同一节点上启动多个 Ascend workload 容器。
- 大模型 case 会按 `requirements.hardware.card_count` 申请更多卡；`ASCEND_LOCK_LABEL` 可作为整机外层保护。
- `reports/` 和 `logs/` 是运行时产物目录，不应提交到 Git。
- PD 分离可在同一容器内通过多个 `services[]` 进程表达；多机拓扑仍预留。
